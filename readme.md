# This mac changer designed for Linux distros

For a now it's just take two arguments -i (interface) and -m(mac_addres)

Example Usage:
  python3  main.py -i eth0 -m 1a:23:54:6b:66:8b

If u see success after the comand , it is done .
